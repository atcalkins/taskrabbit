!!!
%html
%head
  %title TaskRabbit
  = stylesheet_link_tag 'applcation', media: 'all', 'data-turbolinks-track' => true
  = javascript_include_tag  'application', 'data-turbolinks-track' => true
  = csrf_meta_tags

%body
  - Category.all.each do |category|
    = link_to category.nam, tasks_path(category: category.name)

  = yield
